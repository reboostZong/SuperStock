<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:background="@color/colorPrimary"
    android:orientation="horizontal"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <ImageButton
        android:id="@+id/searchbar_left_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginLeft="5dp"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:background="#00000000"
        android:src="@drawable/ic_back"
        android:visibility="visible" />

    <EditText
        android:id="@+id/searchbar_edittext"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toRightOf="@id/searchbar_left_button"
        android:layout_toLeftOf="@id/searchbar_right_button"
        android:layout_marginLeft="5dp"
        android:hint="@string/search"
        android:textColorHint="#B0FCFCFC"
        android:textColor="#FFFFFF"
        android:maxLength="6"
        android:inputType="number"
        android:background="@null"/>


    <ImageButton
        android:id="@+id/searchbar_right_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:background="#00000000"
        android:src="@drawable/ic_clear"
        android:visibility="invisible" />

</RelativeLayout>
